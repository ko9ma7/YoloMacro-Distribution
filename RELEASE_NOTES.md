# YoloMacro Release Notes

## v1.4.5 - 2026-07-28

### 동일 대상 추적과 입력 분리

- `마우스 액션`과 `찾은 동일 대상을 계속 따라가기`를 분리했습니다.
- 클릭·키 입력은 최초 인식 위치에서 기존 설정대로 1회 실행하고, 이후 프레임은 커서 이동만 수행합니다.
- 광학 흐름으로 연속 이동·회전을 추적하고 최초 외형과 신뢰도 높은 중간 외형을 함께 보존해 배경 드리프트를 줄였습니다.
- 점진 투명화·짧은 가림은 최근 속도로 제한적으로 예측하며, 이탈 허용을 넘으면 종료합니다.

### YOLO 하이브리드 재탐지

- 같은 클래스가 여러 개일 때 이전 위치·속도·겹침에 가장 맞는 검출을 동일 대상으로 연결합니다.
- YOLO 모델이 로드되지 않았으면 시각 동일성 추적으로 대체하고 실행 로그에 표시합니다.
- 완전히 투명해 배경과 같은 픽셀이 된 대상은 직접 관측할 수 없다는 한계를 설명서와 UI에 명시했습니다.

### 호환성과 검증

- 기존 `대상 따라가기` 마우스 액션을 새 추적 토글과 `동작 안 함`으로 자동 이관합니다.
- OpenCV·YOLO 추적 내장 템플릿과 일괄 설정 필드를 추가했습니다.
- 이동·회전·점진 투명화 18프레임, 3프레임 가림, 같은 클래스의 먼 방해 대상 회귀 검사를 통과했습니다.
- English release summary: [`docs/RELEASE_NOTES_1.4.5_EN.md`](docs/RELEASE_NOTES_1.4.5_EN.md)

## v1.4.4 - 2026-07-28

### 전용 글자 OCR

- 프로필별로 범용 Tesseract, 전용 글자 샘플, 혼합 자동 선택을 지원합니다.
- 한 글자 ROI를 라벨별로 직접 캡처하거나 여러 PNG/JPG/BMP 파일을 가져와 `OcrData/<프로필>/GlyphSamples/<라벨>/`에 누적합니다.
- 대상 프로그램의 고정 폰트·숫자·짧은 상태 문구를 글자 단위로 분리하고 정규화해 가장 가까운 라벨을 조합합니다.
- 결과는 기존 포함/전체 일치/정규식/숫자 이상·이하 판정과 결과 변수 흐름에 연결됩니다.

### 적응형 대상 추적

- `대상 따라가기` 액션이 최초 검출 이미지의 중심을 화면 이탈까지 따라갑니다.
- 회색조와 외곽선 템플릿을 지역 재탐색하고 성공 프레임으로 기준을 조금씩 갱신해 이동·색 변화에 대응합니다.
- 추적 간격, 최대 시간, 연속 이탈 허용 프레임, 재탐색 반경을 액션별·일괄 설정으로 제공합니다.

### 버전 표시

- 활성화 배너에 설치 버전과 기존 단일 DLL 자동업데이트 정책 버전을 따로 표시합니다.
- 전체 ZIP 설치가 필요한 1.4.4가 정책 버전 1.4.2로 보이던 혼동을 제거했습니다.
- English release summary: [`docs/RELEASE_NOTES_1.4.4_EN.md`](docs/RELEASE_NOTES_1.4.4_EN.md)

## v1.4.3 - 2026-07-28

### OCR 인식

- Tesseract 기반 한국어·영문·숫자 OCR과 재사용 가능한 프로필을 추가했습니다.
- 읽기 성공, 포함, 전체 일치, 정규식, 숫자 이상/이하 판정을 지원합니다.
- 인식 텍스트·숫자·신뢰도와 정규식 이름 그룹을 `{last.*}` 또는 사용자가 지정한 결과 키로 후속 입력·메시지에서 사용할 수 있습니다.
- ROI 즉시 테스트, 전처리, 확대, 반전, 문자 화이트리스트, PSM, 최소 신뢰도 설정을 제공합니다.

### 대표 이미지 사전

- 대상 창 또는 카메라의 현재 ROI를 라벨별 샘플로 저장하고, 가장 가까운 화면 상태를 판정합니다.
- 특정 라벨 탐색 또는 전체 라벨 분류, 색상/회색/윤곽 비교, 다중 크기 허용을 지원합니다.
- 기존 클릭·키 입력·분기·Replay·능동 학습 흐름을 그대로 사용합니다.

### 통합·배포

- 선택 설정 복사, 값 일괄 변경과 기본 템플릿에서 OCR·대표 이미지 사전 설정을 지원합니다.
- 기존 프로젝트 호환성을 위해 출시된 인식 엔진의 숫자 값을 유지했습니다.
- OCR 텍스트/숫자/정규식 그룹/단어 위치와 대표 이미지 분류를 자동 회귀 검사합니다.
- 배포 ZIP에 Tesseract 관리/네이티브 런타임과 한국어·영문 언어 데이터를 포함합니다. 기존 설치는 DLL 하나만 교체하지 말고 전체 ZIP을 새 폴더에 풀어야 합니다.
- English release summary: [`docs/RELEASE_NOTES_1.4.3_EN.md`](docs/RELEASE_NOTES_1.4.3_EN.md)

## v1.4.2 - 2026-07-23

### 폴더/목록 순차 실행

- 액션 성공 후 새 화면을 캡처하더라도 남은 하위 동작을 건너뛰지 않습니다. 폴더 내부와 상위 목록을 화면에 보이는 순서대로 계속하고, 목록 끝에서만 처음으로 돌아갑니다.
- 실행 로그에 캡처 정책, 입력 경로, 동작 전/후 대기와 다음 검사 항목을 표시합니다.

### 고속 클릭과 키 입력

- 한 번 발견한 위치를 최대 10,000회 클릭하는 `고속 반복 좌클릭`을 추가했습니다. 0ms 최속부터 1,000ms 간격까지 설정하고 F6으로 중단할 수 있습니다.
- 비활성 창 입력을 실제 `KEYDOWN → KEYUP` 메시지로 보내며 숫자·문자·Space·Enter·방향키와 Ctrl/Alt/Shift 조합을 처리합니다. Windows 문자 변환과 별도 `CHAR`를 중복 전송하지 않아 숫자가 두 번 입력되지 않습니다.
- 키 감지에서 Space가 빈칸으로 보이지 않고 `{SPACE}`로 저장되며 Interception도 특수키 구문을 실제 키로 변환합니다.

### 작업 이름과 공개 링크

- 실행 강조 `▶`와 폴더 표시 `📁`는 화면에만 표시하고 저장 이름에는 포함하지 않습니다. 기존 프로젝트의 중복 접두사도 불러올 때 정리합니다.
- 프로그램의 `GitHub` 버튼은 설치 파일과 공개 문서가 있는 [YoloMacro-Distribution](https://github.com/ko9ma7/YoloMacro-Distribution)을 엽니다.
- English release summary: [`docs/RELEASE_NOTES_1.4.2_EN.md`](docs/RELEASE_NOTES_1.4.2_EN.md)

## v1.4.1 - 2026-07-22

### 시작 화면과 문서 배포

- 유효한 서명 활성화에서는 저장된 마지막 탭과 관계없이 `RPA 실행` 화면을 시작 화면으로 사용합니다.
- 활성화 확인 중인 임시 상태는 화면을 강제로 바꾸지 않고, 최종 비활성·만료·서명 오류·네트워크/응답 오류·최소 버전 미달 상태에서만 `설정`으로 이동합니다.
- 설정 화면의 수동 `다시 확인`이나 업데이트 확인이 성공해도 사용자가 보고 있던 탭은 강제로 바꾸지 않습니다.
- 시작 화면 선택 규칙을 회귀 테스트에 추가했습니다.
- 소스 저장소와 공개 Distribution 저장소에 한국어·영문 README, 사용자 설명서와 릴리즈 문서를 동기화했습니다.
- English release summary: [`docs/RELEASE_NOTES_1.4.1_EN.md`](docs/RELEASE_NOTES_1.4.1_EN.md)

## v1.4.0 - 2026-07-22

### 실행·활성화·배포

- 서명 활성화 상태 배너와 설정 화면의 재확인 기능을 제공합니다.
- 비활성 상태에서도 DLL·YOLO 모델·대상 창·설정 카메라를 확인하는 로컬 자체 진단을 제공합니다.
- GitHub 브라우저 로그인 상태로 Private 소스 페이지를 여는 작은 버튼을 추가했으며 토큰은 프로그램에 저장하지 않습니다.
- Private 소스/구자료와 공개 배포를 분리하고 `YoloMacro-Distribution`에서 ZIP, DLL, SBOM, 서명 manifest와 한국어·영문 문서를 제공합니다.

### YOLO·AOI·라벨링

- 대상 창과 특정 카메라 입력을 선택해 같은 검사 파이프라인에서 사용할 수 있습니다.
- 라벨 검수/negative 상태, 데이터셋 품질 검사, 동일 이미지 train/val 누수 방지와 YOLO 모델 메타데이터 검증을 강화했습니다.
- YOLO NMS·최대 검출 수, AOI 적응형 허용치·F1 임계값 보정과 복합 판정 흐름을 추가했습니다.

### 보안 업데이트

- ECDSA P-256 서명 manifest, SHA-256 DLL 검증, 별도 updater의 백업·원자 교체·롤백을 적용했습니다.
- 활성화 manifest와 검증된 공개 배포 파일은 [YoloMacro-Distribution](https://github.com/ko9ma7/YoloMacro-Distribution)에 게시합니다.
- English release summary: [`docs/RELEASE_NOTES_1.4.0_EN.md`](docs/RELEASE_NOTES_1.4.0_EN.md)

## v1.3.2 - 2026-07-15

### CPU 회귀 수정

- 검색 기록이 꺼져 있으면 미리보기 상태가 저장되지 않아 모든 항목에서 전체 프레임 복제와 BMP 인코딩이 반복되던 문제를 수정했습니다.
- 미리보기는 항목이나 결과 변화와 관계없이 전역 최대 4FPS만 생성합니다.
- 실행 상태 문구와 작업 강조는 100ms마다 최신 값 하나만 WPF Dispatcher에 전달합니다.

### 실제 프로젝트에 맞춘 이미지 검색

- 캡처는 목록 사이클당 정확히 한 번 유지하면서 ROI 총면적이 작으면 전체 화면 Mat 대신 필요한 ROI만 `LockBits`로 변환합니다.
- ROI와 템플릿 크기가 같은 Exact 검색은 1×1 `MatchTemplate` 대신 동일 점수 체계의 정규화 거리로 직접 비교합니다.
- 작은 ROI 위주의 실행에서는 OpenCV 스레드를 2개로 제한하고 종료 시 원래 스레드 수로 복원합니다.
- OneDrive 프로젝트의 템플릿 파일 변경 검사는 매 검색이 아니라 최대 초당 한 번만 수행합니다.

### Replay 저장 상한과 검증

- Replay 상태 변화는 항목별 최소 10초, 반복 실패는 60초, 전체 JPEG는 최소 2초 간격으로 제한합니다.
- 실제 `nowPANG-15` 관찰 실행 15초 비교에서 전체 CPU 평균은 기존 실행본 `1.73%`, 수정본 `0.63%`였습니다.
- 4,000회 동일 크기 검색에서 직접 비교 경로는 18ms, 기존 `MatchTemplate` 핵심 경로는 338ms였습니다.
- Release 빌드, 이미지 매칭, 캡처, 워크플로, 미리보기 예산, Replay 예산과 실제 UI 관찰 실행을 검증했습니다.

자세한 원인과 측정 조건은 [`docs/CPU_OPTIMIZATION_1.3.2.md`](docs/CPU_OPTIMIZATION_1.3.2.md)를 참고하세요.

## v1.3.1 - 2026-07-15

### 캡처 1회 의미 수정과 CPU 최적화

- `OncePerLoop`를 목록 시작 전체창 1회 캡처로 고정했습니다. 모든 이미지 액션은 성공 여부와 무관하게 자신의 ROI로 같은 첫 프레임을 검사합니다.
- 스마트 대기, 액션 성공, 순방향 이동이 공유 프레임을 폐기하던 경로를 제거했습니다.
- 명시적 `Retry`는 같은 정지 프레임을 반복하지 않고 새 검색 사이클을 시작합니다.
- 첫 프레임과 템플릿의 OpenCV Mat 변환을 캐시하고 ROI Bitmap 복사를 Mat 뷰로 교체했습니다.
- 발견 상태마다 만들던 전체 미리보기를 상태 변화 또는 250ms 주기로 제한했습니다.

### 선택 값 일괄 변경

- 기존 기준 액션 `복사`는 유지하고 별도 `변경` 버튼과 창을 추가했습니다.
- 다중 이미지, 이미지 매칭, 대기·재시도, 마우스 동작, 흐름 범주를 고르면 해당 필드만 표시합니다.
- 각 필드의 `적용`을 체크한 값만 모든 선택 액션에 원자적으로 반영하며 이미지 이름·목록·ROI는 보존합니다.
- 점프 대상, 공유 쿨다운, 실패 대체, 드래그·휠·웹훅·입력 우회와 AI 세부 흐름도 필드 단위로 변경할 수 있으며 대상 없는 점프는 차단합니다.
- 서로 다른 기존 값은 `여러 값`으로 표시하고 새 값을 입력하지 않으면 적용을 차단합니다.

### Replay 감소와 검증

- 동일 실패 저장 간격을 10초에서 60초로 늘리고 ROI 주변만 JPEG로 저장합니다.
- Replay JSONL에는 원본 프레임 크기와 ROI 크롭 좌표를 함께 기록합니다.
- 3개 템플릿이 첫 캡처에만 존재하는 회귀 테스트로 3개 ROI 모두 매칭되고 캡처 수가 정확히 1인지 검증합니다.
- 29개 ROI 성능 스모크에서 공유 Mat 경로가 Bitmap 복제·반복 변환 경로보다 평균적으로 빠른지 확인할 수 있습니다.

자세한 사용법은 [`docs/PERFORMANCE_AND_BULK_EDIT_1.3.1.md`](docs/PERFORMANCE_AND_BULK_EDIT_1.3.1.md)를 참고하세요.

## v1.3.0 - 2026-07-14

### 선택형 일괄 설정

- Ctrl/Shift 다중 선택 후 기준 액션을 우클릭하여 다중 이미지 조건, 이미지 매칭, 대기·중복 방지·재시도, 마우스 및 기타 액션, 흐름을 필요한 그룹만 복사할 수 있습니다.
- 기본 이미지 이름, 다중 이미지 파일 목록과 ROI는 일괄 복사에서 항상 제외합니다.
- 작업 목록의 `일괄`, 액션 메뉴와 우클릭 메뉴에서 같은 기능을 열 수 있습니다.

### 창 사라짐과 투명화 복구

- 저장된 프로그램 창을 현재 모니터 작업 영역 안으로 보정하고 모니터 구성 변경에도 자동 복귀합니다.
- 오버레이가 실패해도 프로그램 창을 다시 표시하고, 도구 메뉴에서 주 모니터 중앙으로 위치를 초기화할 수 있습니다.
- 투명 처리했던 실제 대상 핸들을 별도로 기억하여 새 프로젝트·새 대상 선택·종료·처리되지 않은 예외 전에 원래 스타일과 불투명도로 복구합니다.
- YoloMacro 자체 창 투명화를 차단하고 검은 캡처가 감지되면 즉시 원상 복구합니다.

### Replay 용량과 성능

- 새 설치는 실패 장면만 저장하며, 기존 설정의 저장 모드는 유지합니다.
- 실행별 세션 폴더와 전체 용량·보관 일수 제한을 추가하고 설정 화면에서 현재 용량 확인과 즉시 비우기를 제공합니다.
- 이미지가 사라진 기존 JSONL 행을 정리하고 오래된 세션부터 삭제합니다.
- 매 저장마다 전체 JPG를 정렬하던 작업을 제거하고 100건 또는 약 10MB 단위로 정리합니다.

자세한 사용법은 [`docs/BATCH_SETTINGS_AND_REPLAY_1.3.0.md`](docs/BATCH_SETTINGS_AND_REPLAY_1.3.0.md)를 참고하세요.

## v1.2.2 - 2026-07-11

### 긴 작업 목록의 현재 위치 표시 안정화

- 현재 검사 항목을 `▶ 작업 이름`으로 표시하면서 사용자의 선택과 스크롤 위치는 유지합니다.
- 자동 스크롤은 기본적으로 끄고, 필요할 때만 작업 목록의 `따라가기`를 켤 수 있습니다.
- 관찰 실행의 실제 실행 동일 딜레이와 100ms 표시 갱신 제한은 그대로 유지합니다.
- GitHub 기본 `master` 브랜치와 Release 버전을 함께 갱신합니다.
- 실제 화면과 동작 구분은 [`docs/RUN_LIST_TRACKING_1.2.2.md`](docs/RUN_LIST_TRACKING_1.2.2.md)를 참고하세요.

## v1.2.1 - 2026-07-11

### 관찰 실행 속도와 CPU 사용 개선

- 관찰 실행에서도 각 액션의 `동작 후 대기`를 실제 실행과 동일하게 적용합니다. 이제 입력만 차단되고 실행 타이밍은 그대로 재현됩니다.
- 빠르게 순회할 때 작업 목록 선택·스크롤 요청을 100ms 단위로 묶어 UI 스레드 과부하를 줄였습니다.
- 관찰 토글을 켜면 `관찰 켜짐`과 `입력 없음 · 실제 실행과 같은 대기 적용`을 표시해 현재 모드를 명확히 알 수 있습니다.
- 도움말에 관찰 실행이 차단하는 클릭, 키 입력, 웹훅, AI 전송, 메모리 조작을 구체적으로 표시합니다.

자세한 사용법과 검증 결과는 [`docs/OBSERVE_MODE_1.2.1.md`](docs/OBSERVE_MODE_1.2.1.md)를 참고하세요.

## v1.2.0 - 2026-07-11

### 실행 안전과 진단

- 클릭·키 입력·외부 통신 없이 판정과 흐름만 확인하는 `관찰 실행`을 추가했습니다.
- 실행 전 누락 이미지, ROI 범위, 중복 ID, 삭제된 점프 대상, 순환 경로를 검사합니다.
- 현재 검사 노드와 이동 이유를 실행 화면에 표시합니다.
- 동일 노드 과다 방문, 분당 과다 동작, 최대 연속 실행 시간을 감시해 비정상 반복을 안전 정지합니다.
- 캡처와 입력을 가짜 구현으로 교체할 수 있는 런타임 어댑터 경계를 추가했습니다.
- 상태 변화와 장시간 실패 화면만 제한적으로 저장하는 실행 재생 기록을 추가했습니다.

### 데이터와 모델 관리

- 라벨 누락·범위 오류·너무 작은 박스·중복 이미지·클래스 불균형·train/val 중복을 검사합니다.
- 임계값 근처 화면을 선택적으로 능동 학습 검토함에 수집합니다.
- 프로젝트 누락 이미지를 복구하고 미사용 중복 파일은 삭제하지 않고 `Backups`로 이동합니다.
- YOLO 모델의 SHA-256, 입력 크기, 클래스 목록을 프로젝트별 모델 레지스트리에 저장합니다.

### 보안과 업데이트

- Gemini API 키를 Windows DPAPI로 보호하고 `%LocalAppData%/YoloMacro`에 저장합니다.
- Gemini 키를 URL에서 제거하고 인증 헤더로 전송합니다.
- 프로젝트별 AI 전송, 웹훅, 메모리, DLL 인젝션, 실제 커서와 드라이버 입력 권한을 실행 전에 표시합니다.
- 웹훅은 HTTPS만 허용하며 메시지 내용은 실행 로그에 저장하지 않습니다.
- GitHub 업데이트 ZIP의 SHA-256을 필수 검증하고 적용 전 백업·실패 시 복원을 수행합니다.
- GitHub Actions 빌드/테스트, CodeQL, Gitleaks 비밀정보 검사, Dependabot, CycloneDX SBOM과 빌드 출처 증명을 추가했습니다.
- 유료 Authenticode 인증서는 사용하지 않습니다. 공개 Release와 SHA-256, GitHub 출처 증명으로 무결성을 제공합니다.

## v1.1.0 - 2026-07-10

### 폴더 조건과 프로젝트 이미지

- 폴더에 조건 이미지를 지정할 수 있습니다. 조건 이미지가 보이면 하위 동작을 실행하고, 보이지 않으면 폴더 전체를 건너뜁니다.
- 조건 이미지가 없거나 옵션을 끄면 기존 일반 폴더와 동일하게 동작하므로 기존 프로젝트 실행 방식은 유지됩니다.
- 새 프로젝트는 독립 임시 작업공간과 `Images` 폴더를 사용합니다. 다른 프로젝트 이미지가 새 프로젝트 목록에 누적되지 않습니다.
- 이미지 이름은 충돌이 없으면 읽기 쉬운 원래 이름을 사용하고, 실제 중복일 때만 `-2`, `-3` 접미사를 붙입니다.
- 노드를 삭제할 때 다른 노드에서 사용하지 않는 연결 이미지도 함께 정리합니다.
- 다중 선택 항목을 한 단계 밖 또는 최상위로 이동할 수 있으며 화면 순서를 유지합니다.

### 설정과 템플릿

- 액션 설정은 기본 옵션만 먼저 보여주고 이미지 매칭 세부값, 입력 우회, 대기·재시도는 필요할 때 펼치도록 정리했습니다.
- 폴더 설정에서는 클릭·키 입력을 숨기고 조건 이미지 역할만 명확히 표시합니다.
- `추천 완성형` 템플릿 6종을 먼저 보여줍니다. 폴더 조건 순차 공정, 3상태 화면 라우터, YOLO+AOI 복구, 보상 우선순위, 로딩 안정 대기, 호버 멈춤 복구를 포함합니다.
- 추천 템플릿은 일반 노드와 동일한 이미지, ROI, 흐름 설정으로 구성되어 추가 후 바로 편집할 수 있습니다.

### AOI와 YOLO 라벨링

- 라벨링 도구 이름과 옵션을 한국어로 정리하고 선택한 도구에 필요한 옵션만 표시합니다.
- 객체 자동 선택과 색상 영역 자동 선택, 선택 객체 자동 보정, 이전 이미지 라벨 복사를 제공합니다.
- YOLO 내보내기는 프로젝트별 폴더에 학습/검증 데이터를 분리하고 `data.yaml`과 객체·클래스 개수를 함께 검증합니다.
- 같은 이름의 원본 이미지도 내보내기 과정에서 덮어쓰지 않습니다.

### 대상 창 복구와 검증

- 투명 보관 실패나 캡처 검정 화면 발생 시 투명도와 클릭 통과 속성을 즉시 원상 복구합니다.
- `대상 창 다시 보이기`는 남아 있는 클릭 통과 속성을 강제로 제거하고 창 프레임을 갱신합니다.
- 프로젝트 격리, 폴더 하위 우회, 추천 템플릿 참조, YOLO 학습/검증 분리를 회귀 검증에 추가했습니다.

### 배포

- `YoloMacro-v1.1.0-win-x64.zip`
- SHA-256: `4c867efa8cbb4c71cc50c0995d3ca979fa5aba64aa48fbab12f817e9f064dd06`
- 크기: `44,974,713 bytes`

## v1.0.13 - 2026-07-08

### 추가

- 작업 목록 제목 옆에 빠른 추가 버튼을 추가했습니다.
  - `+`: 새 폴더 추가
  - `A`: 새 액션 추가
  - `T`: 로직 템플릿 추가
- 설정 화면에 GitHub Releases manifest 기반 업데이트 확인 영역을 추가했습니다.
  - 기본 manifest 주소: `https://github.com/ko9ma7/YoloMacro/releases/latest/download/update-manifest.json`
  - `stable`, `beta` 채널 선택 지원
  - 새 버전 감지 시 다운로드 또는 릴리즈 페이지 열기
- 완성형 반복 공정 템플릿을 추가했습니다.
  - 보상 버튼 감지 -> 우선순위 선택 -> 최종 클릭
  - 시작 버튼 클릭 후 로딩 안정 대기
  - 클릭 씹힘/호버 멈춤 복구 세트
  - 팝업/확인창 닫기 우선 처리
- 템플릿 추가 창에 추가될 폴더/액션 미리보기를 추가했습니다.
- `YoloMacro.Core` 핵심 코드를 `YoloMacro.CoreLib.dll`로 분리했습니다.

### 수정

- 창 크기 변경 시 작업목록이 화면을 과도하게 차지하지 않도록 작업목록과 로그 영역의 적응형 최대 크기를 적용했습니다.
- `ClearHoverBeforeCapture` 기본값을 꺼짐으로 되돌려 기존 안정 동작 기준과 맞췄습니다.
- Visual Studio용 `.sln`과 `.slnx`에 CoreLib 프로젝트를 포함했습니다.

### 배포

- `distribution/update-manifest.json`을 추가해 GitHub Releases 자산으로 올릴 수 있게 했습니다.
- 변경 전 소스 백업은 `distribution/backups/`에 생성합니다.
- 새 소스 스냅샷은 `distribution/source/`에 생성합니다.
- 릴리즈 패키지:
  - `YoloMacro-v1.0.13-win-x64.zip`
  - SHA-256: `2398229c0ded897a705c050eea690ab0d57a23293ea4653e6a5bc5a1fad84f6c`
  - 크기: `44,931,295 bytes`
- 소스 스냅샷:
  - `YoloMacro-v1.0.13-source.tar.gz`

## v1.0.12 - 2026-06-23

### 수정

- 일부 게임/앱플레이어가 비활성 `PostMessage` 클릭을 무시하는 경우를 위해 액션 설정에 `실제 커서 클릭 강제` 옵션을 추가했습니다.
- 인터셉션을 켰지만 드라이버가 준비되지 않은 상태에서 화면 좌표와 비활성 클릭 좌표가 섞이던 입력 경로를 보정했습니다.
- 매크로 시작 로그에 성공 후 진행 방식과 기본 입력 방식을 표시하고, 액션 성공 후 다음 검색 항목 또는 다음 캡처 사이클 이동을 로그로 남깁니다.
- OpenCV 이미지 매칭 프리셋에 `빠른 이진화 매칭 (글자/아이콘)`을 추가했습니다.
- 대상 메뉴에 대상 창을 다음 모니터, 주 모니터, 화면 밖으로 이동하는 기능을 추가했습니다.

### 검증

- `dotnet build YoloMacro\YoloMacro.csproj -c Release -p:UseAppHost=false -p:OutDir=artifacts\build-check-release\` 통과

## v1.0.11 - 2026-06-23

### 수정

- v1.0.10의 `템플릿 식별력 부족` 처리가 기존 프로젝트의 작은 버튼, 닫기 버튼, 재연결 버튼까지 막던 문제를 수정했습니다.
- 작은 템플릿은 더 이상 매칭을 중단하지 않습니다. 실행 로그와 테스트 결과에 `템플릿 식별력 낮음` 주의만 표시하고, 기존 방식대로 검색과 클릭은 계속 진행합니다.
- 상대좌표 `화면에서 찍기`도 기준 이미지가 작다는 이유만으로 중단하지 않도록 되돌렸습니다. 실제로 화면에서 못 찾은 경우에만 실패합니다.
- `정확 매칭` 프리셋 설명을 현재 동작에 맞게 수정했습니다. 작은 단색 조각은 오클릭 위험 안내 대상이지 실행 차단 대상이 아닙니다.

### 검증

- `dotnet build YoloMacro\YoloMacro.csproj -c Release -p:UseAppHost=false -p:OutDir=artifacts\build-check-release\` 통과

## v1.0.10 - 2026-06-23

### 수정

- OpenCV `정확 매칭`에 템플릿 식별력 검사를 추가했습니다. 초록 선택 표시, 노란 배경 일부, 얇은 선처럼 너무 작거나 단색에 가까운 조각은 다른 위치에서도 100%가 나올 수 있으므로 매칭 자체를 중단합니다.
- 매크로 실행 로그에 `정확 매칭 중단: 템플릿 식별력 부족` 경고를 1회 표시합니다. 기존 데이터가 고장 난 것이 아니라, 기준 이미지가 대상 전체 특징을 충분히 담지 못한 경우를 구분합니다.
- 설정창의 `테스트` 버튼도 같은 기준을 사용합니다. 차단 시 크기, 색상 수, 주색상 비율, 상세 비율을 보여주고 다시 캡처해야 하는 이유를 안내합니다.
- 상대좌표 `화면에서 찍기` 계산 시 기준 이미지가 너무 단순해서 차단된 경우와 실제로 화면에서 못 찾은 경우를 다른 메시지로 표시합니다.
- 메인 창 기본 크기를 1450x800에서 1180x720으로 줄이고, 액션 설정창 기본 크기를 1050x900에서 920x760으로 줄였습니다. 설정창의 과한 구조 아이콘과 일부 긴 버튼 문구도 정리했습니다.

### 검증

- `dotnet build YoloMacro\YoloMacro.csproj -c Release -p:UseAppHost=false -p:OutDir=artifacts\build-check-release\` 통과
- PANG10 이미지 폴더 기준으로 문제였던 19x40 선택 조각, 24x21 배경 조각, 30x64 단색 조각은 차단되고 121x40 대상 특징 템플릿은 통과함을 확인했습니다.

## v1.0.9 - 2026-06-23

### 수정

- OpenCV `정확 매칭`의 후보 검증을 강화했습니다. 기존 1차 템플릿 점수가 높아도 실제 후보 영역의 색상/배경/픽셀 근접도가 낮으면 실패 처리합니다.
- 노란색 버튼 이미지가 연갈색 배경 후보를 통과하는 것처럼, 단순한 색상 영역에서 발생하던 오클릭 가능성을 줄였습니다.
- `정확 매칭`은 내부적으로 최소 픽셀 유사도와 채널별 근접 픽셀 비율을 함께 확인합니다.
- 탐색 실패 로그를 개선했습니다. 이제 최고점수가 기준보다 높아도 픽셀 검증에서 탈락한 경우 `1차 점수는 높지만 색상/픽셀 검증 실패`로 구분해 표시합니다.
- 설정창의 매칭 테스트 결과도 `검증 통과 점수`와 `1차 최고 점수`를 나눠 보여줍니다.
- 새 OpenCV 액션 기본값을 `정확 매칭 + 92%`로 변경했습니다.
- 기본 OpenCV 템플릿은 정확 매칭으로 시작하고, 밝기 보정/이진화/외곽선 템플릿은 의도적으로 유연한 검색 설정을 유지합니다.
- 메인 화면과 노드 설정창의 긴 탭 이름을 줄여 작은 창에서 잘리는 문제를 완화했습니다.
- MaterialDesignInXAML Toolkit의 `MaterialDesignThemes` 5.3.2 패키지와 기본 Material Design 2 리소스를 적용했습니다.

### 검증

- `dotnet build YoloMacro\YoloMacro.csproj -c Release -p:UseAppHost=false -p:OutDir=artifacts\build-check-release\` 통과
- Debug 빌드는 실행 중인 Visual Studio/앱이 `obj\Debug\net8.0-windows\refint\YoloMacro.dll`을 잡고 있어 파일 잠금 오류가 발생했습니다. 코드 오류가 아니며 Release 빌드로 검증했습니다.

## v1.0.8 - 2026-06-22

### 추가

- 참고자료 폴더의 여러 매크로 소스 패턴을 반영해 OpenCV 액션별 전처리 모드를 추가했습니다.
- 전처리 모드: 밝기 정규화, 히스토그램 평활화, 노이즈 완화, 선명화, 자동 이진화, 적응형 이진화.
- 환경 설정에 `검색 기록 저장` 옵션을 추가했습니다. 켜면 `Logs/vision-search-YYYYMMDD.jsonl`에 ROI, 엔진, 이미지명, 점수, 전처리, 성공 여부를 기록합니다.
- 커스텀 스크립트 명령을 추가했습니다: `랜덤대기 min max`, `변수 key value`, `랜덤문자 key length`, `로그 message`.
- 기본 템플릿을 추가했습니다: 밝기 보정 이미지 검색, 글자/아이콘 이진화 검색, 랜덤 시간 대기, 랜덤 문자열 입력, 수동 이동 루트 목록.

### 검증

- `dotnet build YoloMacro\YoloMacro.csproj -p:UseAppHost=false -p:OutDir=artifacts\build-check\` 통과

## v1.0.7 - 2026-06-22

### 수정

- v1.0.6에서 OpenCV 후보 검증이 과하게 적용되어 기존 프로젝트의 작은 ROI와 `색상 무시` 템플릿이 액션까지 진행되지 못하던 문제를 수정했습니다.
- 저장된 항목의 정확도 값을 다시 기준으로 사용하도록 복구했습니다. 예: 기존 프로젝트의 `정확도 85% + 색상 무시` 설정이 내부에서 96% 이상으로 강제되지 않습니다.
- 후보 검증은 화면 밖 좌표와 명백히 다른 픽셀 후보만 거르도록 완화했습니다.
- 매칭이 성공하면 기존처럼 클릭, 드래그, 키 입력 등 액션 단계로 진행됩니다.

### 검증

- `dotnet build YoloMacro\YoloMacro.csproj -p:UseAppHost=false -p:OutDir=artifacts\build-check\` 통과

## v1.0.6 - 2026-06-22

### 수정

- OpenCV 템플릿 매칭에서 1차 점수만 보고 성공 처리하던 구조를 강화했습니다.
- 후보 위치를 다시 잘라 템플릿과 실제 픽셀 유사도까지 검증합니다.
- `색상 무시`, `모양만 비교`, `마젠타 배경 무시` 옵션 사용 시 엣지 겹침 검증을 추가했습니다.
- 작은 템플릿 또는 낮은 높이의 ROI는 오탐이 많기 때문에 내부적으로 더 엄격한 기준을 적용합니다.
- 거절된 후보의 점수가 성공 로그에 표시되지 않도록, 성공 시에는 실제 통과한 후보 점수를 기록하게 했습니다.

### 검증

- `dotnet build YoloMacro\YoloMacro.csproj -p:UseAppHost=false -p:OutDir=artifacts\build-check\` 통과

## v1.0.5 - 2026-06-22

### 수정

- `1사이클 1캡처` 모드에서 리스트 항목마다 전역 대기를 반복하지 않도록 수정했습니다.
- 실패한 ROI 항목도 `동작 후 대기`를 기다리던 문제를 수정했습니다. 이제 실제 클릭/키/스크립트 액션이 실행된 경우에만 액션 후 대기를 적용합니다.
- 검색 주기와 액션 후 안정 대기를 분리했습니다. 환경 설정에 `검색 사이클 대기(ms)`를 추가했습니다.
- 액션 성공 후 오래된 캡처 화면으로 남은 리스트를 계속 검사하지 않도록, 빠른 스캔 모드에서 성공 시 다음 캡처 사이클로 복귀하는 옵션을 추가했습니다.
- ROI 검색 시 불필요한 전체 화면 Bitmap 복제를 제거하고, 템플릿 이미지를 실행 중 캐시하여 반복 매칭 속도를 개선했습니다.
- 미리보기 갱신을 제한해 검색 루프 중 UI 렌더링 부하를 줄였습니다.
- 환경 설정 화면을 스크롤 가능하게 수정해 작은 창에서도 하단 설정이 잘리지 않게 했습니다.

### 검증

- `dotnet build YoloMacro\YoloMacro.csproj -p:UseAppHost=false -p:OutDir=artifacts\build-check\` 통과

## v1.0.4 - 2026-06-22

### 수정

- OpenCV 이미지 매칭 옵션을 프리셋 중심으로 정리했습니다.
- 프리셋 선택은 체크박스처럼 다시 눌러 꺼지는 구조가 아니라 항상 하나의 매칭 방식이 유지되도록 변경했습니다.
- 고급 옵션을 직접 바꾸면 자동으로 `직접 설정` 프리셋으로 전환하고, 모양 비교처럼 충돌 가능한 옵션은 저장 전에 정규화합니다.
- 선택한 탐색 엔진이 OpenCV가 아니면 OpenCV 전용 매칭 옵션을 비활성화합니다.
- 액션 설정창에 `테스트` 버튼을 추가해 현재 타겟 화면에서 최고 점수, 기준값, 후보 좌표, 실패 원인을 바로 확인할 수 있게 했습니다.
- 메모리 설정에 `프로필 열기`, `스냅샷 열기` 버튼을 추가했습니다.
- 메모리 프로필 예제 생성 시 `MemoryProfile.guide.md`도 같이 생성해 주소/포인터/자료형 입력 흐름을 바로 확인할 수 있게 했습니다.
- `docs/memory-monitor.md`를 한국어 UTF-8 문서로 다시 정리했습니다.

### 검증

- `dotnet build YoloMacro.sln` 통과
- Release 실행 파일 시작 스모크 통과

## v1.0.3 - 2026-06-22

### 수정

- `저장`과 `다른 이름으로 저장`을 분리했습니다. 기존 `저장` 버튼이 매번 새 폴더를 만드는 문제를 정리했습니다.
- 자동저장이 원본 프로젝트 파일을 직접 덮어쓰지 않고 `AutoSave/session-{instanceId}.json`과 `latest.json`에 복구용으로만 저장되도록 변경했습니다.
- 자동저장과 일반 저장 중 `CleanUpImages()`가 실행되어 이미지가 의도치 않게 삭제될 수 있던 흐름을 제거했습니다.
- 미사용 이미지 삭제는 `도구 > 미사용 이미지 정리`에서 사용자가 후보 목록을 확인한 뒤 실행하도록 변경했습니다.
- 프로젝트 저장 시 `Backups` 폴더에 기존 파일을 백업하고, 임시 파일 저장 후 교체하는 원자 저장 방식을 적용했습니다.
- 프로젝트 폴더에 `.ymacro.lock`을 생성해 다중 인스턴스에서 같은 프로젝트를 열 때 읽기 전용/복사본 열기를 선택할 수 있게 했습니다.
- 전역 F5/F6 단축키 등록 실패를 무시하지 않고 로그에 표시하도록 수정했습니다.
- 기존 JSON 프로젝트를 계속 열 수 있도록 `SchemaVersion`과 기본 마이그레이션/정규화 경로를 추가했습니다.

### 검증

- `dotnet build YoloMacro.slnx` 통과
- `ProjectStorageService` 스모크 테스트 통과
- Release 실행 파일 시작 스모크 통과

## v1.0.2 - 2026-06-09

### 수정

- 시작 후 정지 버튼을 누를 때 `YoloMacro.Core.MemoryMonitor.StopAsync()`에서 발생하던 `NullReferenceException`을 수정했습니다.
- 매크로 정지 버튼, 엔진 종료 콜백, 메모리 모니터 비활성화 처리에서 `StopAsync()`가 동시에 호출되어도 동일한 `CancellationTokenSource`를 중복 해제하지 않도록 생명주기 잠금을 추가했습니다.
- 메모리 프로필 JSON에서 `Watches`가 누락되거나 빈 목록이어도 예외 대신 안내 메시지로 처리하도록 방어했습니다.

### 문서

- GitHub 첫 화면의 `README.md`를 한국어 UTF-8 문서로 재작성했습니다.
- 메모리 모니터 설정 문서를 최신 구조에 맞게 정리했습니다.
- GitHub manifest 기반 업데이트와 비공개 배포 운영 방식을 문서화했습니다.

### 배포

- 프로젝트 버전을 `1.0.2`로 갱신했습니다.
- `distribution/v1.0.2` 폴더에 배포용 manifest와 zip 산출물을 둘 수 있도록 구조를 정리했습니다.

## v1.0.1

- AOI, YOLO, OpenCV 혼합 로직 템플릿을 이미지 리스트에 추가하는 구조를 도입했습니다.
- 템플릿을 카테고리/폴더식으로 선택하고 사용자 템플릿을 저장/삭제할 수 있게 개선했습니다.
- 좌표 설정에서 대상 창 기준 절대 좌표와 찾은 이미지 중심 기준 상대 좌표를 구분했습니다.
- GitHub manifest 기반 업데이트 확인 구조를 추가했습니다.
